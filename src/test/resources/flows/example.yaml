id: example
namespace: io.kestra.milvus

tasks:
- id: date
  type: io.kestra.plugin.milvus.Example
  format: "{{taskrun.startDate}}"
- id: task-id
  type: io.kestra.plugin.milvus.Example
  format: "{{task.id}}"
- id: flow-id
  type: io.kestra.plugin.milvus.Example
  format: "{{outputs['task-id'].child.value}}"
